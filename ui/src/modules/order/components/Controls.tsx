import Button from 'ui/Button';

const OrderControls = () => {
  return (
    <div className="order-controls">
      <Button className="take">Авч явах</Button>
      <Button className="pay">Төлбөр төлөх 42 500₮</Button>
    </div>
  );
};

export default OrderControls;
