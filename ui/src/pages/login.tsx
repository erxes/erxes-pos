import LoginContainer from 'modules/auth/containers/Login';
import Layout from 'modules/auth/components/LoginLayout';

const Login = () => {
  return <LoginContainer />;
};

Login.Layout = Layout;

export default Login;
